# nackademin.nackbutik - ASP.NET Core 1.0 Server

No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)

## Run

Linux/OS X:

```
sh build.sh
```

Windows:

```
build.bat
```

## Run in Docker

```
cd src/nackademin.nackbutik
docker build -t nackademin.nackbutik .
docker run -p 5000:5000 nackademin.nackbutik
```
